% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-battery.R
\name{simulate_mapping_noise_null}
\alias{simulate_mapping_noise_null}
\title{Simulate the mapping-noise null for one branch}
\usage{
simulate_mapping_noise_null(
  query_genes,
  background_genes,
  attrition_rate,
  R = 1000,
  seed = NULL
)
}
\arguments{
\item{query_genes, background_genes}{the \emph{native}-namespace gene lists
before mapping (attrition rate is applied directly to these; this
function does not perform ID translation).}

\item{attrition_rate}{the branch's observed attrition rate (methods note
§6), applied identically to \code{query_genes} and \code{background_genes}.}

\item{R}{number of replicates (default 1000; methods note §7).}

\item{seed}{optional RNG seed for reproducibility.}
}
\value{
a list of length \code{R}, each element a list with \code{query} and
\code{background} (the noise-perturbed gene lists), ready to pass into
\code{\link[=run_enrichment_battery]{run_enrichment_battery()}}.
}
\description{
Methods note §7: draws \code{R} replicates of pure random gene dropout at the
branch's \emph{observed} attrition rate (independent of which genes real
mapping actually drops), to give a null distribution against which
observed instability can be judged "in excess of chance."
}
