% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragility-lookup.R
\name{build_fragility_table}
\alias{build_fragility_table}
\title{Build the reusable per-pathway fragility lookup table}
\usage{
build_fragility_table(sig_table, dataset, db, mode, tau_lo = 0.1, tau_hi = 0.9)
}
\arguments{
\item{sig_table}{a data.frame with one row per \verb{(pathway_id, branch)}
combination and columns \code{pathway_id}, \code{branch}, \code{testable} (logical),
\code{significant} (logical). Callers assemble this from
\code{\link[=run_enrichment_battery]{run_enrichment_battery()}} output across all branches for one fixed
\verb{(dataset, db, mode)}.}

\item{dataset, db, mode}{labels stamped onto every output row, so tables
from multiple runs can be row-bound into the full lookup table.}

\item{tau_lo, tau_hi}{passed to \code{\link[=classify_fragility]{classify_fragility()}}.}
}
\value{
a data.frame with columns \code{dataset}, \code{db}, \code{mode}, \code{pathway_id},
\code{k_p}, \code{F}, \code{H}, \code{V}, \code{class}.
}
\description{
The tool's headline community-facing artifact (plan §6, §9): one row per
\verb{(pathway, dataset, db, mode)} with its fragility score, entropy index,
and stable/fragile classification, exported flat so it can be inspected
directly (CSV/TSV) rather than only read out of a figure.
}
