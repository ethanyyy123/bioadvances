% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-metrics.R
\name{rank_concordance}
\alias{rank_concordance}
\title{Rank concordance of an enrichment statistic across two branches}
\usage{
rank_concordance(stat_a, stat_b)
}
\arguments{
\item{stat_a, stat_b}{named numeric vectors (names = pathway IDs) of the
ranking statistic (\code{-log10(p_adj)} for ORA, \code{NES} for GSEA) in each
branch. Entries absent from a vector, or \code{NA}, are treated as untestable.}
}
\value{
a list with \code{rho} (Spearman correlation) and \code{n} (number of
pathways used).
}
\description{
Restricted to pathways testable (non-\code{NA} statistic) in both branches, per
the methods note §3 — untestable pathways are excluded, not imputed.
}
