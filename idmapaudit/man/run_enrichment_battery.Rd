% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-battery.R
\name{run_enrichment_battery}
\alias{run_enrichment_battery}
\title{Run the standard ORA/GSEA battery for one ID-mapping branch}
\usage{
run_enrichment_battery(
  query,
  background,
  ranked_stat = NULL,
  dbs = c("GO_BP", "KEGG", "Reactome", "WikiPathways"),
  modes = c("ORA", "GSEA"),
  org_db = "org.Hs.eg.db",
  fdr_cutoff = 0.05
)
}
\arguments{
\item{query}{character vector of mapped, branch-specific significant gene
IDs.}

\item{background}{character vector of mapped, branch-specific background
gene IDs (plan §5.3: redefined per branch, not reused from another
branch's namespace).}

\item{ranked_stat}{named numeric vector (names = gene IDs in this
branch's namespace) of the DESeq2 test statistic, for GSEA mode. May be
\code{NULL} if \code{modes} excludes \code{"GSEA"}.}

\item{dbs}{character vector, subset of \code{c("GO_BP", "GO_MF", "GO_CC", "KEGG", "Reactome", "WikiPathways")}.}

\item{modes}{character vector, subset of \code{c("ORA", "GSEA")}.}

\item{org_db}{the annotation package/object to pass through to
clusterProfiler (e.g. \code{org.Hs.eg.db::org.Hs.eg.db}), or a string such as
\code{"org.Hs.eg.db"}.}

\item{fdr_cutoff}{FDR threshold used only to label results; full result
tables are always returned unfiltered so callers can recompute at a
different threshold (plan §8 threshold-sensitivity check).}
}
\value{
a named list keyed \code{"<mode>.<db>"}, each element a data.frame with
at least columns \code{pathway_id}, \code{p_adjust}, \code{stat} (\code{-log10(p_adjust)}
for ORA, \code{NES} for GSEA), and \code{significant} (logical, at \code{fdr_cutoff}).
}
\description{
Thin, swappable wrapper around clusterProfiler/ReactomePA/fgsea (plan
§5.3). Requires the relevant Bioconductor packages, which are declared in
\code{Suggests} rather than \code{Imports} because they are heavy and not needed for
the pure stability-metric logic in this package; callers get an explicit
error naming the missing package rather than a cryptic failure.
}
