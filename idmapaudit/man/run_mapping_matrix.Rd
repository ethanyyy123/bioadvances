% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping-matrix.R
\name{run_mapping_matrix}
\alias{run_mapping_matrix}
\title{Build the full ID-mapping matrix for a DE result}
\usage{
run_mapping_matrix(
  query_genes,
  background_genes,
  resolvers,
  resolution_policies = list()
)
}
\arguments{
\item{query_genes, background_genes}{character vectors of native
(unversioned Ensembl) gene IDs from the fixed DE step.}

\item{resolvers}{a named list of resolver functions. Each resolver is
called as \code{resolver(genes)} (the union of \code{query_genes} and
\code{background_genes}) and must return a two-column \code{from}/\code{to}
data.frame in that gene's \emph{raw, unresolved} mapping (i.e. it may
contain duplicate \code{from} keys) if the resolver's namespace has a
resolution policy applied afterward, or an already-resolved table
otherwise (e.g. plain Ensembl version-stripping has no ambiguity to
resolve).}

\item{resolution_policies}{a named list, subset of \code{names(resolvers)},
mapping resolver name to the policy/policies to apply, e.g.
\code{list(entrez = c("first", "list"), symbol_orgdb = c("first", "list"))}.
A resolver not named here is used as-is (already resolved). \code{"list"}
here is shorthand for \code{\link[=resolve_mapping]{resolve_mapping()}}'s \code{"all"} policy (kept as a
distinct exploded-rows branch, per plan §5.1 item 4).}
}
\value{
a named list, one entry per resulting branch (resolver name, or
\code{"<resolver>__<policy>"} when a policy was applied), each the output of
\code{\link[=apply_mapping_branch]{apply_mapping_branch()}}.
}
\description{
Orchestrates the primary factor (namespace) x secondary factor
(resolution policy, applied only where the plan calls for it — Entrez and
Symbol) described in plan §5.1. Namespace resolvers that require
Bioconductor packages (\code{org.Hs.eg.db}, \code{biomaRt}) are supplied by the
caller as \code{resolver} functions so this function, and its branch-selection
logic, stay testable without those packages installed; see
\code{vignettes/airway-prototype.R} for the concrete resolvers used against the
airway dataset.
}
