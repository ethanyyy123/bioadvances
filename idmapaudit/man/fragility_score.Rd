% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-metrics.R
\name{fragility_score}
\alias{fragility_score}
\title{Audit / fragility score for a single pathway}
\usage{
fragility_score(sig, testable = !is.na(sig))
}
\arguments{
\item{sig}{logical vector: one entry per ID-mapping branch, \code{TRUE} if the
pathway was called significant in that branch.}

\item{testable}{logical vector, same length as \code{sig}: \code{TRUE} if the
pathway had at least one mapped gene in that branch. Branches where
\code{testable} is \code{FALSE} are excluded from the denominator entirely (they
are not counted as "not significant"). Defaults to \code{!is.na(sig)}.}
}
\value{
a list with \code{F} (fragility proportion, \code{NA} if no branch is
testable), \code{H} (binary entropy of \code{F}), \code{V} (Bernoulli variance of \code{F}),
and \code{k_p} (number of testable branches).
}
\description{
Implements methods note §4: the fraction of testable branches in which a
pathway is called significant, plus the entropy- and variance-based
fragility indices that treat "always in" and "always out" symmetrically as
stable, and mid-range as fragile.
}
