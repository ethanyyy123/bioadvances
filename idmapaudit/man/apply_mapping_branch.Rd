% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping-matrix.R
\name{apply_mapping_branch}
\alias{apply_mapping_branch}
\title{Apply an ID-mapping branch to a query gene list and its background,
keeping the two redefinitions separable}
\usage{
apply_mapping_branch(query_genes, background_genes, mapping)
}
\arguments{
\item{query_genes, background_genes}{character vectors of native-namespace
(pre-mapping) gene IDs.}

\item{mapping}{a data.frame with columns \code{from}, \code{to} — the \emph{resolved}
mapping (i.e. already passed through \code{\link[=resolve_mapping]{resolve_mapping()}}) covering both
\code{query_genes} and \code{background_genes}.}
}
\value{
a list with \code{query} (mapped query gene IDs), \code{background} (mapped
background gene IDs), \code{query_attrition}, \code{background_attrition}.
}
\description{
Plan §5.3: a gene that fails to map is invisible to both the query list
and the background, and these two effects must be reported separately
rather than conflated. This function returns both mapped sets plus the
attrition rate of each, computed independently.
}
