% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping-matrix.R
\name{resolve_mapping}
\alias{resolve_mapping}
\title{Resolve a one-to-many/many-to-one ID mapping under a resolution policy}
\usage{
resolve_mapping(mapping, policy = c("drop", "first", "all"))
}
\arguments{
\item{mapping}{a data.frame/list with columns \code{from} and \code{to} (character),
one row per raw mapping edge. A \code{from} key with zero rows means it
failed to map at all (attrition, not ambiguity).}

\item{policy}{one of \code{"drop"} (remove any \code{from} key with more than one
distinct \code{to} value), \code{"first"} (keep the first \code{to} value per \code{from}
key, in input row order), or \code{"all"} (keep every edge, exploding
one-to-many keys into duplicate rows).}
}
\value{
a data.frame with columns \code{from}, \code{to}, deduplicated per policy.
}
\description{
Pure-logic core of the ID-mapping step (plan §5.1 secondary factor):
given a raw mapping table (possibly containing duplicate keys, e.g. one
Ensembl gene mapping to several Entrez IDs), collapse it under one of
three policies. This function takes a plain mapping table rather than
calling \code{org.Hs.eg.db}/\code{biomaRt} directly, so the resolution-policy logic
can be unit-tested without those (large) Bioconductor dependencies
installed.
}
